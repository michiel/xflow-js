var should = require('chai').should();
var xflow  = require('../lib/xflow');

describe('xflow', function() {
    it('loads a json flow', function() {
        xflow().should.equal('');
      });
  });

